[pytest]
# Register custom marks so pytest doesn't warn about unknown ones
markers =
    db: marks tests that require a running Neo4j instance (deselect with -m "not db")

# Default options applied to every pytest run
addopts =
    --strict-markers
    --tb=short

# Test discovery
testpaths = tests

# asyncio mode for any async tests
asyncio_mode = auto